output {
  redis {
    host => [{% for host in groups.cache %} "{{ host }}" {%- if not loop.last %},{% endif %}{% endfor %}]
    congestion_threshold => "1000000"
    #data_type => "channel"
    data_type => "list"
    #key => "logstash-%{type}-%{+yyyy.MM.dd.HH}"
    key => "logstash-log"
  }
}
